<header class="site-header">
    <h1 class="text-lg">StoryCrafter AI</h1>
    <style>
        .site-header {
            background-color: #2c2f52;
            /* deep indigo */
            color: #ffffff;
            padding: 1rem 2rem;
            text-align: center;
            font-size: 1.8rem;
            font-weight: bold;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
            position: relative;
            z-index: 10;
            margin-bottom: 1rem;
        }

        .site-header h1 {
            margin: 0;
            letter-spacing: 0.5px;
        }

        body {
            background-color: var(--color-background);
        }
    </style>
</header>