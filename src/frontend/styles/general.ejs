<style>
  /* Base: Mobile-first */
  .text-xs {
    font-size: 0.75rem;
  }

  /* 12px */
  .text-sm {
    font-size: 0.875rem;
  }

  /* 14px */
  .text-md {
    font-size: 1rem;
  }

  /* 16px */
  .text-lg {
    font-size: 1.25rem;
  }

  /* 20px */
  .text-xlg {
    font-size: 1.5rem;
  }

  /* Desktop overrides using min-width media query */
  @media (min-width: 768px) {
    .text-xs {
      font-size: 0.875rem;
    }

    /* 14px */
    .text-sm {
      font-size: 1rem;
    }

    /* 16px */
    .text-md {
      font-size: 1.125rem;
    }

    /* 18px */
    .text-lg {
      font-size: 1.5rem;
    }

    /* 24px */
    .text-xlg {
      font-size: 2rem;
    }

    /* 32px */
  }

  :root {
    /* Primary brand color - deep purple header */
    --color-primary: #3b3a61;
    /* custom deep muted indigo */
    --color-primary-light: #5c5b86;
    --color-primary-dark: #2c2a4f;

    /* Secondary accent - warm golden notification */
    --color-secondary: #f59e0b;
    /* Amber-500 */
    --color-secondary-light: #fbbf24;
    --color-secondary-dark: #b45309;

    /* Backgrounds */
    --color-background: #fff7dd;
    /* warm creamy yellow */
    --color-surface: #ede8ff;
    /* soft lavender for cards */
    --color-surface-muted: #e2dcfa;
    /* muted lavender alt */

    /* Text colors */
    --color-text: #2f2f44;
    /* deep grayish purple for contrast */
    --color-text-muted: #6c648b;
    /* for descriptions */
    --color-text-inverse: #ffffff;

    /* Accent and borders */
    --color-accent: #f59e0b;
    /* reused secondary */
    --color-border: #cfcde5;
    /* light lavender-gray for soft borders */
  }

  .horizontal-padding {
    padding-left: 1.5rem;
    padding-right: 1.5rem;
  }

  .title {
    text-align: center;
  }

  .list {
    display: flex;
    flex-direction: column;
    justify-content: center;
    color: var(--color-text);
  }
</style>