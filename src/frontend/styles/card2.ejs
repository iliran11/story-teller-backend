<style>
  .card-2 {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 1rem;
    width: 100%;
  }

  .card-2 .card {
    background: linear-gradient(135deg, var(--color-surface) 0%, var(--color-surface-muted) 100%);
    border-radius: 1.5rem;
    padding: 2rem;
    width: 100%;
    max-width: 400px;
    box-shadow: 
      0 8px 32px rgba(59, 58, 97, 0.12),
      0 2px 8px rgba(59, 58, 97, 0.08);
    border: 1px solid var(--color-border);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
  }



  .card-2 .card:hover {
    transform: translateY(-8px);
    box-shadow: 
      0 20px 40px rgba(59, 58, 97, 0.16),
      0 8px 16px rgba(59, 58, 97, 0.12);
  }

  .card-2 .card-title {
    color: var(--color-primary);
    font-weight: 600;
    margin: 0 0 1.5rem 0;
    letter-spacing: -0.02em;
    line-height: 1.3;
  }

  .card-2 .tags-container {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    justify-content: flex-start;
  }

  .card-2 .tag {
    background: var(--color-primary);
    color: var(--color-text-inverse);
    padding: 0.375rem 0.875rem;
    border-radius: 2rem;
    font-weight: 500;
    letter-spacing: 0.025em;
    transition: all 0.2s ease;
    border: none;
    white-space: nowrap;
    position: relative;
    overflow: hidden;
  }

  .card-2 .tag::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transition: left 0.5s ease;
  }

  .card-2 .tag:hover {
    background: var(--color-primary-light);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(59, 58, 97, 0.3);
  }

  .card-2 .tag:hover::before {
    left: 100%;
  }

  .card-2 .tag.tag-contrast {
    background: var(--color-secondary);
    color: var(--color-text);
  }

  .card-2 .tag.tag-contrast:hover {
    background: var(--color-secondary-light);
    color: var(--color-text);
  }

  /* Responsive adjustments */
  @media (max-width: 480px) {
    .card-2 .card {
      padding: 1.5rem;
      max-width: 100%;
      margin: 0 0.5rem;
    }
    
    .card-2 .tags-container {
      justify-content: center;
    }
  }

  /* Accessibility improvements */
  .card-2 .tag:focus {
    outline: 2px solid var(--color-primary);
    outline-offset: 2px;
  }

  /* Material Design ripple effect */
  .card-2 .card {
    position: relative;
    cursor: pointer;
  }

  .card-2 .card::after {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 0;
    height: 0;
    border-radius: 50%;
    background: rgba(59, 58, 97, 0.1);
    transform: translate(-50%, -50%);
    transition: width 0.3s ease, height 0.3s ease;
    pointer-events: none;
  }

  .card-2 .card:active::after {
    width: 300px;
    height: 300px;
  }
</style> 