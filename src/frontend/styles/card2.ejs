<style>
  .card-2 {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 0.75rem;
    width: 100%;
  }

  .card-2 .card {
    background: var(--color-surface);
    border-radius: 1rem;
    padding: 1.25rem;
    width: 100%;
    box-shadow: 0 2px 8px rgba(59, 58, 97, 0.08);
    border: 1px solid var(--color-border);
  }

  .card-2 .card-title {
    color: var(--color-primary);
    font-weight: 600;
    margin: 0 0 1rem 0;
    line-height: 1.4;
  }

  .card-2 .tags-container {
    display: flex;
    flex-wrap: wrap;
    gap: 0.375rem;
  }

  .card-2 .tag {
    background: var(--color-secondary);
    color: var(--color-text);
    padding: 0.2rem 0.6rem;
    border-radius: 1.5rem;
    font-weight: 400;
    min-height: 28px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .card-2 .tag:focus {
    outline: 2px solid var(--color-primary);
    outline-offset: 2px;
  }

  /* Skeleton loading styles */
  .card-2 .skeleton-title {
    height: 1.75rem; /* Match text-lg line height */
    width: 75%; /* Typical title width */
    background: linear-gradient(90deg, #dcdcdc 25%, #b8b8b8 50%, #dcdcdc 75%);
    background-size: 200% 100%;
    animation: skeleton-loading 2.5s infinite;
    border-radius: 0.375rem;
    margin-bottom: 1rem;
  }

  .card-2 .skeleton-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.375rem;
  }

  .card-2 .skeleton-tag {
    height: 28px; /* Match tag min-height */
    background: linear-gradient(90deg, #dcdcdc 25%, #b8b8b8 50%, #dcdcdc 75%);
    background-size: 200% 100%;
    animation: skeleton-loading 5.5s infinite;
    border-radius: 1.5rem; /* Match tag border-radius */
  }

  .card-2 .skeleton-tag:nth-child(1) {
    width: 70px;
    animation-delay: 0s;
  }

  .card-2 .skeleton-tag:nth-child(2) {
    width: 85px;
    animation-delay: 0.2s;
  }

  .card-2 .skeleton-tag:nth-child(3) {
    width: 55px;
    animation-delay: 0.4s;
  }

  @keyframes skeleton-loading {
    0% {
      background-position: -200px 0;
    }
    100% {
      background-position: calc(200px + 100%) 0;
    }
  }
</style> 